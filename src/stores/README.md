# Stores Directory

Contains global Zustand state management stores.
Stores should be modular and focus on client-only global concerns (e.g. auth status, workspace selection state, sidebar status, global modal layouts).
Features should house their own feature-scoped state stores inside their respective folders.
