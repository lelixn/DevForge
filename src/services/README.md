# Services Directory

Contains global API clients, third-party integrations, and web service configurations (e.g. Socket.io, Sentry, Monaco, etc.).
The primary configuration for HTTP queries is defined in `api.ts`.
