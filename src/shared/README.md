# Shared Directory

Place highly generic, non-feature-specific UI components, helper utilities, and shared constants here.
These are elements that are reused throughout the application and have no dependencies on specific features.
