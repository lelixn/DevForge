# Types Directory

Contains global typescript interface and type definitions.

- `index.ts` houses common platform models like Workspaces, Projects, Users, Sprints, and Issues.
- Feature-scoped types should remain inside their respective feature directory.
